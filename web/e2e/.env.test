# E2E Test Environment Configuration
# This configuration runs tests against a real backend with a test database

# Backend Configuration
BACKEND_URL=http://localhost:8787
DATABASE_URL=postgres://sidechain:test_password@localhost:5433/sidechain_test

# Playwright Configuration
PLAYWRIGHT_TEST_BASE_URL=http://localhost:5173

# Stream.io Configuration (required for social features)
# These should be set via GitHub Actions secrets or environment
STREAM_API_KEY=${STREAM_API_KEY}
STREAM_API_SECRET=${STREAM_API_SECRET}

# Test Database Configuration
POSTGRES_USER=sidechain
POSTGRES_PASSWORD=test_password
POSTGRES_DB=sidechain_test

# Redis Configuration
REDIS_URL=redis://localhost:6380

# JWT Configuration for Backend
JWT_SECRET=test_jwt_secret_for_testing_only
