localhost:80 {
    root * /app/dist
    encode gzip

    # SPA routing - serve index.html for all routes
    try_files {path} /index.html

    file_server

    log {
        output stdout
        format json
    }
}
